GameTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
    - dynamodb:Query
  Resource:
    - ${self:custom.GameTable.arn}
    - !Join ['/', ['${self:custom.GameTable.arn}', 'index', 'slug']]
